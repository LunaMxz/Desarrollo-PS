Unica capa que puede importar `pool` de `config/db.js` y escribir SQL.

Un archivo por tabla o agregado relacionado, ej: `usuarios.repository.js`,
`cargos.repository.js`. Si el dia de mañana cambian de base de datos,
solo esta carpeta se modifica.
